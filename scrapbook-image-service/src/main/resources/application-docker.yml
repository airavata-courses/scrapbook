spring:
  data:
    mongodb:
      host: ${MONGO}
      port: 27017
      database: image-service
      repositories:
        enabled: true

scrapbook:
  googledrive:
    service:
        baseurl: ${DRIVE}
  metadata:
        baseurl: ${MDE}