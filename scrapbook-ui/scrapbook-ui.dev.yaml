apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: http-sb-ui
  name: http-sb-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: http-sb-ui
  template:
    metadata:
      labels:
        app: http-sb-ui
    spec:
      containers:
      - image: chiragindi/scrapbook-staging-ui
        name: http-sb-ui
        imagePullPolicy: Always
        ports:
        - containerPort: 80
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: http-sb-ui
  name: http-sb-ui
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30000
  selector:
    app: http-sb-ui
