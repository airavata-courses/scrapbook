apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: http-sb-metadata
  name: http-sb-metadata
spec:
  replicas: 1
  selector:
    matchLabels:
      app: http-sb-metadata
  template:
    metadata:
      labels:
        app: http-sb-metadata
    spec:
      containers:
      - image: chiragindi/scrapbook-staging-metadata-service
        name: http-sb-metadata
        imagePullPolicy: Always
        ports:
        - containerPort: 12000
        env:
          - name: DEBUG
            value: "True"
          - name: PORT
            value: "12000"
          - name: MONGO_URI
            value: "mongodb://149.165.172.158:27017/scrapbook-staging-mde"
          - name: KAFKA_URI
            value: "http-sb-kafka:9092"
          - name: KAFKA_TOPIC
            value: "image"
          - name: G_DRIVE_SERVICE
            value: "http://http-sb-gdrive:9090"
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: http-sb-metadata
  name: http-sb-metadata
spec: 
  type: ClusterIP
  selector: 
    app: http-sb-metadata
  ports:
  - port: 12000
    targetPort: 12000
    nodePort: 30005
